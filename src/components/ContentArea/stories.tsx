import { Story, Meta } from '@storybook/react'
import ContentArea from '.'

export default {
  title: 'Content Area',
  component: ContentArea,
} as Meta

export const Template: Story = () => <ContentArea />
